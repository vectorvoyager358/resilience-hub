# Direct backend dependencies. Regenerate the lockfile with:
#   ./scripts/compile-requirements.sh
#
# Backend API (`python -m server.app` → Flask + Pinecone)
flask>=3.0,<4
flask-cors>=6.0,<7
python-dotenv>=1.0,<2
pinecone>=9.1.0,<10
gunicorn>=26.0.0,<27
firebase-admin>=7.5.0,<8
google-cloud-firestore>=2.28.1,<3
google-genai>=2.17.0,<3
cohere>=7.0.8,<8
langfuse>=4.14.3,<5

# Security constraints for transitive dependencies. Keep these floors at or
# above the versions accepted by pip-audit.
cryptography>=50.0.0,<51
h2>=4.4.1,<5
pyasn1>=0.6.4,<0.7
